apiVersion: apps/v1
kind: Deployment
metadata:
  name: work-pool-payment-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: work-pool-payment-service
  template:
    metadata:
      labels:
        app: work-pool-payment-service
    spec:
      containers:
        - name: work-pool-payment-service
          image: work-pool-payment-service:local
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8084
          envFrom:
            - configMapRef:
                name: work-pool-shared-config
            - secretRef:
                name: work-pool-shared-secret
            - secretRef:
                name: work-pool-payment-service-secret
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8084
            initialDelaySeconds: 60
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8084
            initialDelaySeconds: 90
            periodSeconds: 20
          startupProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 30
---
apiVersion: v1
kind: Service
metadata:
  name: work-pool-payment-service
spec:
  selector:
    app: work-pool-payment-service
  ports:
    - name: http
      port: 8084
      targetPort: 8084

