apiVersion: apps/v1
kind: Deployment
metadata:
  name: work-pool-notification-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: work-pool-notification-service
  template:
    metadata:
      labels:
        app: work-pool-notification-service
    spec:
      containers:
        - name: work-pool-notification-service
          image: work-pool-notification-service:local
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8083
          envFrom:
            - configMapRef:
                name: work-pool-shared-config
            - secretRef:
                name: work-pool-shared-secret
            - secretRef:
                name: work-pool-notification-service-secret
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8083
            initialDelaySeconds: 60
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8083
            initialDelaySeconds: 90
            periodSeconds: 20
          startupProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 30
---
apiVersion: v1
kind: Service
metadata:
  name: work-pool-notification-service
spec:
  selector:
    app: work-pool-notification-service
  ports:
    - name: http
      port: 8083
      targetPort: 8083

