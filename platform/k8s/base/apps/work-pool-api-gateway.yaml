apiVersion: apps/v1
kind: Deployment
metadata:
  name: work-pool-api-gateway
spec:
  replicas: 1
  selector:
    matchLabels:
      app: work-pool-api-gateway
  template:
    metadata:
      labels:
        app: work-pool-api-gateway
    spec:
      containers:
        - name: work-pool-api-gateway
          image: work-pool-api-gateway:local
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: work-pool-shared-config
            - secretRef:
                name: work-pool-shared-secret
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 60
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 90
            periodSeconds: 20
          startupProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 30
---
apiVersion: v1
kind: Service
metadata:
  name: work-pool-api-gateway
spec:
  selector:
    app: work-pool-api-gateway
  ports:
    - name: http
      port: 8080
      targetPort: 8080

