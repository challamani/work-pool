apiVersion: networking.istio.io/v1beta1
kind: Gateway
metadata:
  name: work-pool-gateway
spec:
  selector:
    istio: ingressgateway
  servers:
    - port:
        number: 80
        name: http
        protocol: HTTP
      hosts:
        - api.work-pool.org
        - ui.work-pool.org
        - user-service.work-pool.org
        - kiali.work-pool.org
        - prometheus.work-pool.org
---
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: work-pool-api-vs
spec:
  hosts:
    - api.work-pool.org
  gateways:
    - work-pool-gateway
  http:
    - match:
        - uri:
            prefix: /
      route:
        - destination:
            host: api-gateway
            port:
              number: 8080
---
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: work-pool-ui-vs
spec:
  hosts:
    - ui.work-pool.org
  gateways:
    - work-pool-gateway
  http:
    - match:
        - uri:
            prefix: /
      route:
        - destination:
            host: work-pool-ui
            port:
              number: 80
---
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: work-pool-user-service-vs
spec:
  hosts:
    - user-service.work-pool.org
  gateways:
    - work-pool-gateway
  http:
    - match:
        - uri:
            prefix: /
      route:
        - destination:
            host: user-service
            port:
              number: 8081
---
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: work-pool-kiali-vs
spec:
  hosts:
    - kiali.work-pool.org
  gateways:
    - work-pool-gateway
  http:
    - match:
        - uri:
            prefix: /
      route:
        - destination:
            host: kiali.istio-system.svc.cluster.local
            port:
              number: 20001
---
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: work-pool-prometheus-vs
spec:
  hosts:
    - prometheus.work-pool.org
  gateways:
    - work-pool-gateway
  http:
    - match:
        - uri:
            prefix: /
      route:
        - destination:
            host: prometheus.istio-system.svc.cluster.local
            port:
              number: 9090
